plugins {
    id 'java'
}

dependencies {
    compileOnly project(':EssentialsApi')
}

jar {
    from(project(':EssentialsApi').sourceSets.main.output)
}

java {
    withSourcesJar()
}

processResources {
    filesMatching('**/*.yml') {
        expand(project.properties)
    }
}

